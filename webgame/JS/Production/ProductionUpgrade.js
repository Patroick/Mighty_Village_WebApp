class ProductonUpgrade {

    generatingValue;
    price;
    generationIncrease;
    priceIncrease;
    amount;

    constructor(productionType) {
        generatingValue += geneartingValue * productionType.generationIncrease;
        price += price * productionType.priceIncrease;
        amount += 1;
    }

}